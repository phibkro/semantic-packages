# Rust realization placeholder

This directory will contain one realization of the Stack specification and a thin adapter exposing the specification vocabulary to the shared conformance runner.

Do not add representation requirements to the specification merely because this implementation chooses a particular Rust layout or ownership strategy.

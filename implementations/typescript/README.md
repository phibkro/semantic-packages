# TypeScript realization placeholder

This directory will contain an independent realization of the Stack specification and a thin adapter exposing the specification vocabulary to the shared conformance runner.

The implementation should not copy the Rust algorithm mechanically; independence helps differential and conformance testing expose underspecification.
